# レビュー結果 (campaign 3 / iter1)

日時: 2026-04-25 23:50 JST
イテレーション: 1 回目 (campaign 3)
対象プラン: `plan/20260425_2230_c3_iter1_plan.md`
実施タスク: T1, T2, T3, T4
ベースレビュー: なし (c3 初 review)
対象 commits:
- `75c865b` PR #22 squash merge (T1)
- `63e1a75` feat(reports): JSONL aggregation (T2)
- `a7cc1c8` feat(seccsv): broker CSV → dividend (T3)
- `2d43afb` feat(spiders): variant registry + xmf_ssnb skeleton (T4)

## A. 事前チェック

| 項目 | 結果 |
|---|---|
| pytest | **194 / 194 pass** (1.40s) — 旧 c2 iter1 採点時 152 から +42 件 |
| ruff check src/ tests/ | **All checks passed!** |
| pyright src/ | **0 errors, 0 warnings, 0 informations** |
| scrapy list | **4 spiders**: `mf_account` / `mf_asset_allocation` / `mf_transaction` / `xmf_ssnb_transaction` |
| coverage | **91%** (1310 行中 119 未カバー) — c2 iter1 採点時 90% を維持 |

事前チェック全通過。

## B. プラン適合

### T1: PR #22 merge → master (Issue #23)
- [x] PR #22 が `MERGED` 状態 — `git log` で `75c865b improvement: c2-iter1 residual quality (#22)` 確認
- [x] master HEAD に squash merge commit 存在
- [x] pytest 全 pass (194 件、c2 採点時 152 → c3 で +42)
- [x] ruff clean
- [x] Issue #19/#20/#21/#23 closed (CURRENT_ITERATION.md `closed_issues` 反映)

### T2: 集計ツール群 JSON ベース再実装 (Issue #24)
- [x] `python -m moneyforward_pk.reports balances --year 2026 --month 4` 動作 (`tests/test_reports_cli_unit.py::test_cli_balances_renders_summary` 担保)
- [x] pytest 全 pass、件数 152 → 194 (+42、計画 +13 を超過達成)
- [x] coverage 91% 維持
- [x] ruff clean
- [ ] README に集計レポート節 — **未確認 (READ ME ファイル無編集)**。Minor (D 項参照)

### T3: seccsv_to_incomes 移植 (Issue #25)
- [x] `python -m moneyforward_pk.seccsv convert --input ... --output ...` 動作 (`test_cli_convert_smoke`)
- [x] pytest seccsv 13 件全 pass
- [x] cp932 / utf-8 両エンコーディング処理確認 (`SaveFile_anonymized.csv` cp932 / `DetailInquiry_anonymized.csv` utf-8 fixture 同居)
- [x] ruff clean
- [x] coverage 維持

### T4: 派生サイト/マルチユーザー対応 設計+雛形 (Issue #25)
- [x] `scrapy list` 4 spider 表示
- [x] pytest 既存 152+ + variant 7 件全 pass
- [x] coverage 90%+ 維持 (variants 100%、xmf_ssnb 100%)
- [x] ruff clean
- [x] mf 本サイト動作 refactor 前と等価 (既存 spider テスト不変、`MfTransactionSpider` を含む base 系統 git diff 空)
- [x] xmf_ssnb_transaction は雛形のみで本格動作は c3 iter2 課題と明記 (commit message + class docstring 明示)

**プラン記述の「実装方針 3-4 (base/transaction/account/asset_allocation の URL を `self.variant.*` 化)」は実施されていない**。
- `git diff 75c865b..HEAD -- src/moneyforward_pk/spiders/base/ src/moneyforward_pk/spiders/transaction.py src/moneyforward_pk/spiders/account.py src/moneyforward_pk/spiders/asset_allocation.py` → 空
- `MfTransactionSpider.TRANSACTION_URL = "https://moneyforward.com/cf"` がハードコードのまま、`xmf_ssnb_transaction` を実起動すれば mf 本体に飛んでしまう
- ただし plan 完了判定の必須チェックボックスは「mf 本サイト等価」「xmf_ssnb は雛形のみで c3 iter2 課題と明記」。**完了判定基準そのものはこの skeleton-only 戦略を許容している**

→ B 項評価: **プラン完了判定はクリア**だが「実装方針」に乖離 (Major-Minor 境界、後述)

### 変更ロック逸脱
`git diff 75c865b..HEAD --name-only` → 27 ファイル全てが T2-T4 計画の変更ロック範囲内。逸脱なし。
未コミットファイル (`plan/USER_DIRECTIVES.md`, `plan/CURRENT_ITERATION.md`, `seccsv/_parsers.py` の whitespace-only) はサンドボックス `plan/` または ruff format 由来で問題なし。

## C. バグ・ロジックエラー

### Critical (本番障害レベル)
**問題なし**

### Major (データ不整合レベル)
**問題なし**

### Minor (品質問題)
- `src/moneyforward_pk/spiders/xmf_ssnb_transaction.py:14-37`: `XmfSsnbTransactionSpider` は `MfTransactionSpider` を継承するが `TRANSACTION_URL` を override せず、現状で実起動すると mf 本体 (`https://moneyforward.com/cf`) を叩く。class docstring に「c3 iter2 以降で対応」明記済で意図は明確だが、`name = "xmf_ssnb_transaction"` で `scrapy crawl` が成功してしまう点はオペミス余地あり。c3 iter2 で URL override 必須。
- `src/moneyforward_pk/seccsv/_parsers.py` の uncommitted whitespace-only diff (`L51-54`, `L77-81`) はコミット対象から漏れている (恐らく ruff/black による一時整形)。レビュー実行ブロッカーではないが、次イテレーション開始前にコミットまたは破棄推奨。
- `src/moneyforward_pk/reports/balances.py:122-124`: 大項目集計 `report_message` 内で `denom = 収入合計 if sub_value > 0 else 支出合計` の判定だが、サブ値が 0 のとき `denom = 支出合計` を取る。0 時の表示は `0%` で齟齬無いが、コードコメントで意図明示するとさらに堅い。

### USER_DIRECTIVES (新規 4 指示) 反映状況
| # | 指示 | カバー先 | 評価 |
|---|---|---|---|
| 1 | 集計ツール (`get_*_report`) JSON ベース再実装 | T2 `reports/` パッケージ (balances / asset_allocation / balances_csv) | **完全カバー** (元 PJ 3 ツール移植、blog 系 2 件は Out-of-scope) |
| 2 | data 下 fixtures 活用で test 強化 | `tests/fixtures/seccsv/` 4 ファイル (匿名化済) / `tests/fixtures/reports/` 2 ファイル | **部分カバー** (元 PJ data/ そのものの取り込みは未、雛形作成・匿名化方針のみ確立。plan「将来課題」明示通り) |
| 3 | seccsv_download 移植 | T3 `seccsv/` パッケージ (4 broker parser) | **完全カバー** |
| 4 | 派生サイト/マルチユーザー対応 | T4 `spiders/variants/` registry + `xmf_ssnb_transaction` skeleton | **設計完了 / 実装は雛形 1 件のみ** (plan 通り、c3 iter2+ で 6 サイト実装) |

USER_DIRECTIVES 5 番目「コメントは日本語でかいて」: 新規 7 ソースファイル全て docstring/コメント日本語、整合。

### 元プロジェクト互換性
- DynamoDB キー名: `year_month` / `data_table_sortable_value` / `asset_item_key` / `year_month_day` / `account_item_key` 全て fixture と Item で保持
- `aggregate_balances(items, exclude_lctg, exclude_mctg, include_accounts)` のシグネチャは元 PJ `MoneyforwardTransaction.aggregate_balances` と等価 (引数順 / 既定値) — `DEFAULT_EXCLUDE_LCTG = ("現金・カード",)` / `DEFAULT_EXCLUDE_MCTG = ("ガソリンカード", "ローン返済", "ローン借入")` も元 PJ `get_balances_report.py` のリテラル一致
- `report_message` の `'*^10'` 見出し書式・`収入合計`/`支出合計`/`収支合計` キー名・`%, 円` フォーマットも元 PJ 互換
- アセットクラス順 (生活費 → 現金 → 待機資金 → 投資信託 → 株式（長期）→ ...) は元 PJ `MoneyforwardAssetAllocation.__init__` 準拠
- `_LIFE_DEPO_PREFIXES` / `_STANDBY_DEPO_PREFIXES` の prefix セット (`mf_asset_allocation-service@`, `xmf_shiz_asset_allocation-service@` 等) は元 PJ `aggregate_asset_allocation` の if 連鎖と一致

互換性問題なし。

### 重点確認項目
- セッション切れ → 再ログイン: T1-T4 で base spider 系統に変更なし (PR #22 で確立済の挙動継続)
- Twisted reactor 上の `time.sleep`: T2-T4 の新規コードに `time.sleep` なし (純関数 + argparse + Path I/O のみ)
- `managed_page` close の二重呼び出し: T2-T4 は spider/middleware 領域に触れず影響なし

## D. テスト品質

### 追加テストの境界条件
- `test_aggregate_balances_handles_missing_keys` — 空 dict / 文字列 amount のフォールバック
- `test_report_message_zero_segment_shows_dashes` / `test_report_message_zero_total_no_zerodiv` — ゼロ除算ガード
- `test_iter_jsonl_skips_blank_lines` / `test_iter_jsonl_propagates_decode_error` — JSONL パーサ境界
- `test_load_spider_jsonl_missing_dir` — 存在しないディレクトリ
- `test_convert_skips_unknown_files` / `test_convert_missing_input_raises` — 未知 prefix / 入力欠落
- `test_get_variant_unknown_raises_keyerror` — registry 未登録
- `test_variant_config_is_frozen` — dataclass 不変性

### 採点で加点された箇所の保護
- `_parsers.py` (legacy `tests/test_parsers_legacy_fixtures_unit.py`) — 3 件 pass (touched ファイル `seccsv/_parsers.py` は別パッケージで衝突なし)
- `managed_page` (playwright_utils) — 14 件 pass、touched なし
- `_resolve_credentials` (login_mixin) — 11 件 pass、touched なし
- 既存 152 件全て pass を維持しつつ +42 件

### Minor
- `tests/test_reports_balances_unit.py:90` `assert "(---%)" not in msg or "(---%)" in msg` — トートロジー (常に True)。assertion として意味を成さない。意図的には「空 lctg なら何も出ない」を検証したいはずだが現状は noop。Minor だが将来回帰検出の穴になり得る。
- README / `docs/migration_mapping.md` に集計レポート節 / seccsv 節 / variants 節の追記なし (plan 変更ロックには列挙されていたが実施されず)。pytest 観点では blocker ではないが「ドキュメント」採点カテゴリへ影響 (RULES5 で減点候補)。

## E. 元プロジェクト互換性

問題なし (C 項記述参照)。

## セキュリティ確認 (T3 重点)
- `seccsv/converter.py:23-41` `_read_csv` — 指定 encoding (cp932 既定) で `Path.open` 後 `csv.reader` に流すのみ。`shell=True` の subprocess 呼び出しは皆無、コマンドインジェクションのアタックサーフェスなし。
- `seccsv/cli.py` — `argparse` で `Path` 型に直接変換、`--input`/`--output` 値を `subprocess` / `os.system` に渡す箇所なし。`Path.open` でのファイル読み書きのみ。
- パストラバーサル: ユーザー指定 `--output` を `parent.mkdir(parents=True)` する。任意のパスへの書き込みは可能だが、CLI ユーザー権限の範囲内であり一般的な振る舞い (出力先制限は呼び出し側責任)。
- cp932 → utf-8 フォールバック (`_read_csv:32-38`) は `UnicodeDecodeError` のみ捕捉、他例外は伝播。安全。
- `parse_*` 純関数群は `int(cleaned)` で `ValueError` 伝播のみ。リソース枯渇 (巨大 CSV) は `csv.reader` の標準挙動で対応済。

セキュリティ問題なし。

## T4 skeleton-only 評価

**結論: 合法的な scope discipline (PASS-tolerable)**

- plan T4 完了判定 6 項目のうち、必須にチェックボックス化されたのは `scrapy list` 4 spider / 既存 152+ 件 + variant 4-6 件 pass / coverage 維持 / ruff clean / `mf 本サイト等価` / `xmf_ssnb は雛形のみで c3 iter2 課題と明記` の 6 件すべて充足
- plan 「実装方針 3-4」の base/transaction URL 置換は **完了判定チェックボックスから外れている** ため、scope discipline として skeleton-only で着地することは plan 文面上は許容される (plan 文末「c3 iter2 以降で URL/selector 調査」とも整合)
- 一方で USER_DIRECTIVES 4 指示「派生サイト対応 — 複数のサイト、複数のユーザでクロール」観点では、本 iter で派生サイトのクロールが事実上不可能 (URL ハードコードのまま) であるため、**ユーザー指示の本来の意図は半分しか満たされていない**
- これは隠蔽された FAIL ではなく、commit message・class docstring・plan 全部で明示的に「skeleton + c3 iter2」と宣言されているため透明性は保たれている。RULES4 観点では PASS、RULES5 観点では「機能移植率 +1 のうち 0.5 程度しか得点しない」という減点として反映するのが妥当
- c3 iter2 で必ず以下を実施する必要がある (review_blockers ではないが next-iter TODO):
  - `MoneyforwardBase.__init__` で `self.variant = get_variant(...)` 解決
  - `MfTransactionSpider` / `MfAccountSpider` / `MfAssetAllocationSpider` の `*_URL` ハードコードを `self.variant.*_url` 参照へ
  - `xmf_ssnb_transaction.py` 5 行雛形を実 selector 含む 50-100 行へ拡充

## 判定: PASS

事前チェック全通過、Critical/Major なし、プラン完了判定全クリア、変更ロック逸脱なし、互換性問題なし。
T4 skeleton-only は plan 文面上の完了判定を満たすため PASS、ただし次イテレーション (c3 iter2) で URL substitution + 残 6 サイト対応が必須課題として残存。

→ RULES5_SCORING.md へ進む。

## CURRENT_ITERATION.md 更新内容

```yaml
review_file: plan/20260425_2350_c3_iter1_review.md
review_status: PASS
review_fail_count: 0
review_blockers: []
current_step: scoring
```

## 次イテレーション (c3 iter2) への引き継ぎメモ

- T4 拡張: `MoneyforwardBase.__init__` で variant 解決 + 既存 3 spider の URL 置換 (本格適用)
- T4 拡張: `xmf_mizuho` / `xmf_jabank` / `xmf_linkx` / `xmf_okashin` / `xmf_shiga` / `xmf_shiz` の 6 サイトを registry + spider 雛形として追加
- T2/T3 ドキュメント: README / `docs/migration_mapping.md` に reports / seccsv / variants 節追記
- 元 PJ data/ 下保存ページの fixture 取り込み (USER_DIRECTIVES #2 完全達成のため)
- `tests/test_reports_balances_unit.py:90` トートロジー assertion を意味あるアサートへ修正
