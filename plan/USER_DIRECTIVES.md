# USER_DIRECTIVES.md — ユーザー指示書

ループ実行前にここを編集する。RULES2 がプラン作成時に必ず読む。
採点基準より**ここに書いた指示が優先される**。

---

## 設計変更指示 (実装に反映せよ)

<!-- 例:
- DynamoDB 出力をやめて JSON ファイル出力に変更する
- ログ形式を structlog に統一する
- Python 3.12 以上を前提にする
-->

- 元PJで個別に集計ツールとして実装していた機能群がごっそりない、関連するtablesもまったくなくなっている、これらで実装していた集計やレポート(get_****)は必須であるJSONデータを使って集計できるようにしたい。
- 元PJにはdata下にfixturesの材料になるページが保存されている、test強化したりできるはずだ
- 元PJのseccsv_downloadも重要なツールだ。証券会社の情報をもとにmkoneyforwardでは取得できない配当情報のデータを生成している。
- コメントは日本語でかいて
- 元PJであった様々なMoneyforward派生サイトへの対応が無視されている。複数のサイト、複数のユーザでクロールする必要がある点が無視されてしまっている。これは必要なものなのだ。元々は継承とMakefileで対応していたが、もっとスマートな方法でもいい。

## スコープ変更 (Out-of-scope / In-scope の上書き)

<!-- 例:
- xmf_ssnb だけは今回 In-scope にする
- レポート生成スクリプトは永久に Out-of-scope とする
-->

(未設定)

## 品質基準の追加 (採点カテゴリに加算)

<!-- 例:
- JSON 出力が tests/output/ に書き出せることを確認するテストを必須とする
-->

(未設定)

## 優先度の上書き

<!-- 例:
- T_xxx (job_runner修正) より先に T_yyy (JSON出力) を実施せよ
-->

(未設定)

## その他メモ

<!-- ループに関係ないメモも自由に書いてよい -->

(未設定)

---

**更新日**: (編集時に記入)
**次ループで反映予定**: (何を変えたいか一言)
