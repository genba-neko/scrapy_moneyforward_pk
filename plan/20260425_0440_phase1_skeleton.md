# Phase 1: スケルトン + 依存定義

実施日時: 2026-04-25 04:40 JST
所要: 約5分
コミット: `024938a`, `cd3d77e`, `7afa963`, `5fd5064`

## 目的
Scrapy + Playwright プロジェクトの土台を作る。元 `scrapy_moneyforward` (Splash)
と参考 `scrapy_smbcnikko_pk` (Playwright) の解析を踏まえ、再構築方針を確定。

## 実施内容

### 1. 3プロジェクト並列解析 (Opus 4.7 × 3 agent)
- 元 `scrapy_moneyforward` 構造抽出
  - `src/moneyforward/` 単一パッケージ + Lua scripts (`lua_script.py`)
  - 3 spider 種 × 10+ パートナーポータルバリアント
  - selectors / login flow / Item shape 完全把握
  - DynamoDbPipeline + Splash 中間部
- 参考 `scrapy_smbcnikko_pk` 構造抽出
  - `src/smbcnikko/` レイアウト
  - `PLAYWRIGHT_CONTEXTS = {}` 空意図 (storage_state 注入)
  - middleware 4 層 (HtmlInspector / MaintenanceGuard / PersistentSession / PlaywrightSession)
  - Passkey/WebAuthn 認証 (smbcnikko 固有, MoneyForward では不要)
  - 状態機械 / デーモン (smbcnikko 固有, 不要)
- 対象 `scrapy_moneyforward_pk` 現状把握
  - workbench scaffolding のみ, src/ 不在
  - CLAUDE.md は Flutter 用で誤配置 (放置)
  - 計画ファイルは空 (本作業で埋める)

### 2. 再構築方針の決定
- src/moneyforward_pk/ レイアウトで smbcnikko_pk 流に揃える
- 状態機械 / Passkey / デーモンは省略 (read-only クロール用途)
- パートナーポータル (xmf_*) は Mixin で拡張ポイント化, 具象は v1 未提供
- 2FA 非対応 (元プロジェクトも未対応)
- Playwright 移行表 (Lua → coroutine) を計画書へ記載

### 3. ファイル作成 / 更新
- `plan/20260425_0438_rebuild_scrapy_moneyforward.md` (再構築計画)
- `requirements.txt` 更新
  - 旧: コメントのみ
  - 新: `scrapy>=2.11`, `scrapy-playwright>=0.0.34`, `playwright>=1.40`,
        `Twisted<25`, `python-dotenv`, `boto3`, `slackweb`,
        `python-dateutil`, `pytest`, `pytest-cov`
- `pyproject.toml` 更新
  - `target-version = "py310"`, `select` に `B` 追加
  - `tests/**` の `S101 / S603` 許可
  - `pyright` extraPaths = `["src"]`, venvPath = `.venv-win`
  - pytest filterwarnings 追加
- `src/scrapy.cfg` 新規 (`default = moneyforward_pk.settings`)
- `src/moneyforward_pk/__init__.py` 空ファイル

### 4. コミット
1. `chore: bootstrap project infra (workbench submodule, CI, editor config)`
2. `docs: add Scrapy+Playwright rebuild plan` (placeholder)
3. `build: pin Scrapy + Playwright deps, tighten pyproject config`
4. `docs: fill in rebuild plan body` (内容追加)

## 学び・判断
- 計画ファイルが IDE オープン中で初回 Write 反映されず → 後続コミットで補完
- pyproject.toml の pyright `venvPath/venv` を smbcnikko_pk と同形式に揃え
  ローカル開発で型チェック走るように整備
